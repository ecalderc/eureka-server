spring:
  application:
    name: eureka-server
  config:
    import: "optional:configserver:https://config-server-ecalderc.azurewebsites.net"
    config:
      enabled: true
      uri: http://localhost:8888
      profile: dev